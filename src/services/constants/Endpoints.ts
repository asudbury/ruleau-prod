export const API_URL = "/api/v1";

export const PROCESSES = `/processes`;
export const PROCESS_OVERRIDES = `${PROCESSES}/{0}/overrides`;
export const PROCESS_RULES = `${PROCESSES}/{0}/rules`;
export const CASES = "/processes/{0}/cases";
export const CASE = `${CASES}/{1}`;
export const CASE_OVERRIDES = `${CASES}/{1}/overrides`;
